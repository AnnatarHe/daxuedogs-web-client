'use strict'
import Vue from 'vue'
import {expect} from 'chai'

describe('admin index page test', () => {
    describe('just a yield test', () => {
        const foo = () => true
        expect(foo()).to.be.true
    })
})
